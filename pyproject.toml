[project]
name = "BrasilEmbers"
version = "0.1.0"
description = "Project for Online Analytical processing of Brazilian wildfire data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyspark>=4.0.1",
]
